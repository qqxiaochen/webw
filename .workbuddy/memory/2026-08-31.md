# 2026-08-31

## 深度分析 v4（产出 `.workbuddy/reports/project-deep-analysis-20260831-v4.html`）
对源码 + 线上做了完整体检，HEAD `a9e124e`。共 5 个 P0 / 11 个 P1 / 13 个 P2。

### 本次新发现（v3 报告之后出现的）
- **P0 音乐自动播放回归**（2026-08-29 17:07 改动，已上线）
  - `index.html:740-742` 页面加载即 `initAudio(); tryPlay();`
  - `index.html:714-720` 还有 `document.addEventListener('click', handler, {once:true})` —— 点页面任意位置都会触发播放，比原始 autoplay 更激进
  - `audio.preload='auto'` → 3.1MB mp3 首屏即拉取
  - 08-25 重写时明确移除过，本次是**回归**
  - **回滚源**：`index.html.bak-20260829-1707` 仍是「懶加載，默認關閉」的正确版本（该文件已被 git 跟踪并推到远端）
- **P0 `pay.paomfz.com` 返回 403** —— index 上 2 个 Purchase 按钮（导航 + Hero）全指向它，主转化路径断裂。疑似 Cloudflare 规则误伤，需排查
- **P0 `mall.909net.cn` 不可达**（curl 000）—— Hero「店鋪」按钮目标
- **CDN 边缘节点劣化**：08-29 是 `HKG`+`japaneast`+`cache-nrt-*`；现在是 **`DEL`+`centralindia`+`cache-bom-*`**（香港/东京 → 德里/孟买）。East Asia → South Asia，中文用户延迟上升。属平台调度抖动，仓库侧无法修

### 实测数据（2026-08-31）
- 部署链路不变：用户 → Cloudflare（反代）→ GitHub Pages（Fastly）。`X-GitHub-Request-Id` / `Via: 1.1 varnish` / `X-Fastly-Request-ID` 齐全，链路健康
- `Last-Modified: Sat, 29 Aug 2026 09:29 GMT` → 确认 08-29 17:07 的改动**已上线**
- 下载包正常：`we/5C72CA.zip` 30,474,399 B、`ph/9D759C3.zip` 32,612,175 B，均支持 Range
- VN 死链**依旧 404**（`down/vn/` 目录本地根本不存在）
- `robots.txt` 返回 200 但是 **Cloudflare 注入的 content-signals 模板**（1248 B 全是注释，零有效规则）；`sitemap.xml` 404
- 安全响应头依旧全空，且有非预期的 `Access-Control-Allow-Origin: *`
- `qqxiaochen.github.io/webw/` 无法直连判定（网络干扰）

### 量化结果
- **死文件 69.0 MB / 129.2 MB（53%）**：`down/ph/7A638484.zip`(31MB) + `down/speed/`×3(各 12.7MB)
  - ⚠ `down/speed/` 三份 **MD5 完全相同 `12042ea173`**，同一文件复制三遍，纯浪费 25.4MB
- `static/images/` 13 个文件（≈785KB）**全部零引用**
- **git：1.5GB / 1433MB 是历史大 blob**；55 个 >1MB、46 个 >25MB；`down/` 历史 47 个版本（ph 31 + we 15 + speed 1）；**`in-pack: 0` 从未 gc**，463 loose / 220 目录
- **CSS 重复 51%**：download 16,614 字符中 6,171 字符与 index **选择器+声明完全相同**（64 同名选择器 / 61 条完全一致）
- 三页对照：404.html 是唯一缺 DOCTYPE 的；download.html 缺 h1（`.dl-head-en` 是 span）；404 用 h5 当主标题、0 处 aria-label、无 main/nav/footer、`lang="en"` 但正文中文、viewport 禁用缩放
- 10 个 `target="_blank"` 缺 rel（index 6 + download 4）
- `.workbuddy/` 有 **32 个文件**被 git 跟踪（10 张截图 PNG + 6 个 tmp + 4 份报告 + 6 个 memory 日志 + 6 其他）

### 建议路线（已写入报告第 06 节）
当天可完成且立竿见影的前三步：① 删自动播放（5min）② VN 卡片禁用下载 + 解绑整卡点击（10min）③ 删 CI 里的 `rm -f CNAME`（5min）。
仓库瘦身分两档：只 `git gc --aggressive`（低风险，压 20-40%）；彻底方案 `git filter-repo` 剥离 `down/` 历史（重写全部 hash，高风险，需 mirror 备份）。

## 待办（用户尚未决定执行）
以上均为分析结论，**未改动任何源码**。等用户确认后再动手。
