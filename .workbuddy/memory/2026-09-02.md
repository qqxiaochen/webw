# 2026-09-02 工作日志

## 项目系统分析（架构 / 模块 / 技术栈 / 质量 / 债务）
- 产出结构报告：`.workbuddy/reports/project-architecture-analysis-20260902.html`
- 关键实测数据：CSS 重复 53%（217/410 行 index↔download 完全相同）；.git 1.5GB（in-pack:0）；down/ 130MB 仅 2 个 ZIP 在用；static/images 13 文件零引用
- 线上探针（已验证）：pay.paomfz.com → 403；mall.909net.cn → 000 失联；VN 下载包 → 404（本地无 down/vn/）
- 4 项 P0：① CI `rm -f CNAME`（每次部署删自定义域名凭证）② pay 403 ③ mall 失联 ④ VN 死链 + 音乐自动播放回归（index.html:740-742）
- 代码质量亮点：设计令牌化、IIFE 隔离、rAF 节流、prefers-reduced-motion、零依赖；短板：DRY 差、缺 h1/aria、外链缺 noopener
- 改进路线图分三阶段：止血(P0)→减负(P1:抽 site.css/清死资源/git gc/安全头/可访问性)→提质(P2)

## 浏览器兼容性评估（追加）
- 产出：`.workbuddy/reports/browser-compatibility-analysis-20260902.html`
- 3 处 C1 严重故障：① `.reveal` 默认 opacity:0 依赖 JS 加 .in，但用 NodeList.forEach + IE 无 IO → IE 整页不可见 ② `inset` 简写用于 body::before 等背景层 → Chrome<87/Safari<14.1 背景塌陷 ③ `aspect-ratio`+.video-frame iframe 100% → Chrome<88 视频 0 高 ④ 404.html 缺 DOCTYPE 触发怪异模式
- C2：backdrop-filter(Firefox<103 无模糊)、clamp、flex gap、CSS Grid(IE 单栏)、background-clip:text 渐变字、closest()
- 降级要点：reveal 默认可见 + .js 门控；inset 展开四向；video padding-top:56.25% hack；404 补 DOCTYPE/lang/viewport；clamp 前补固定值；@supports not backdrop-filter 实底

## 执行 4 项 C1 兼容补丁（已落地）
- index.html / download.html：① `.reveal` 默认 `opacity:1`，隐藏与 delay 改到 `.js .reveal` 门控；head 内联 `<script>` + IIFE 顶部 `document.documentElement.className+=' js'` 防 FOUC ② 全部 `inset` 简写展开为 top/right/bottom/left 四向（背景光斑/玻璃高光/modal 共 13 处）③ `.video-frame` 加 `@supports not (aspect-ratio)` 用 `padding-top:56.25%` 比例 hack，iframe 改绝对定位 inset:0 ④ 所有 `NodeList.forEach` 改为 `Array.prototype.forEach.call`（index 2 处 + download 4 处），消除 IE 抛错致整页不可见
- 404.html：补 `<!DOCTYPE html>`、`lang="zh-CN"`、viewport 放开缩放、`wrap-bg` 的 inset 展开
- 验证：grep 确认无残留 `inset:` / `NodeList.forEach` / `lang="en"` / `user-scalable=0`；仅 IntersectionObserver 回调的 `entries.forEach`（真数组）保留；现代浏览器渲染路径不变

## 404.html 顶部标签栏按钮修复（已落地）
- 问题诊断：① 窗口控制按钮仅 14×14px、关闭标签页图标仅 11×11px，远小于推荐触控目标 ② `.tab i` 在 flex 容器内使用 `float:right` + `margin-top` 定位，垂直对齐不可靠 ③ 所有图标按钮为空 `<a>`，无 `aria-label`/`role` ④ hover 反馈弱（仅 opacity:.7），无 active 反馈 ⑤ `href="javascript: void(0);"` 含空格且语义不清
- 修复：窗口按钮扩至 32×32px（图标仍 14×14 居中）、tab-add 扩至 30×30px、tab close 扩至 18×18px；改用 flex 居中 + `calc(50%)` 绘制图标；加 `role="button"` 与中文 `aria-label`；补 `:hover`/`:active` 背景/缩放反馈，关闭窗口按钮 hover 变红；保留原旋转动效同时修复 active 时旋转丢失（`.btn-close:active{transform:rotate(45deg) scale(.93)}`）
- HTML 标签栏格式化并注入无障碍属性；CSS 结构平衡验证通过

## index.html TOS 板块补充简体中文翻译（已落地）
- 仅更新 `#tosModal` 内 `<ol>` 的 9 条英文条目：每条 English 后 `<br>` + 内联 muted 的 `<span lang="zh-CN">` 中文译文（display:block; margin-top:5px; color:var(--text-3); font-size:13px），复用站点双语文案弱化样式
- 未改动标题 `<h3>TERMS OF SERVICE</h3>`、`<h4>服務條款</h4>`、模态结构、CSS 与 JS 逻辑；编号圆形 `::before` 计数器正常工作

## TERMS OF SERVICE 卡片滚动条移除（已落地）
- 根因：`.modal-window` 原 `max-height:82svh;overflow:auto`，补充中文翻译后内容增高触发滚动条
- 修复：`.modal-window` 改为 `overflow:visible` 并去掉 `max-height`（卡片随内容增长，正常屏无滚动）；两层加隐藏滚动条安全网（`.modal` 与 `.modal-window` 均 `scrollbar-width:none`/`-ms-overflow-style:none`/`::-webkit-scrollbar{display:none}`），极短屏仍可滚动但无可见滑块
- 玻璃样式、圆角、开合动画、背景遮罩、JS 逻辑均不变

## index.html 移动端适配修复（已落地，纯移动端专属，桌面版未动）
- 问题定位（手机版缩放异常/布局错位/内容模糊）：
  1. `.hero::after` 装饰光扫 `left:-20%` 向左溢出视口 → 触发移动端 zoom-to-fit 模糊/裁切（根因）
  2. `.hero h1` `clamp` 下限 3rem(48px) 过大，320–360px 屏 "PaoMian Hax" 横溢/裁切
  3. `--blur` 在移动端仍 18px，磨砂压在文字上显虚
  4. `.modal` 用 flex `align-items:center` 居中，TOS 内容高于短屏时顶部被裁且滚不到（flex 溢出已知 bug）
  5. section/footer/modal 留白在手机上偏松
- 修复（全部在 `@media (max-width:760px)` / `430px`，未触及桌面规则）：
  - `viewport` meta 加 `viewport-fit=cover`（刘海安全区，桌面无影响）
  - `.hero` 加 `overflow:hidden`（裁掉左溢光扫；桌面视觉不变）
  - 760 块：`.hero{padding:124px 18px 60px}`、`.hero-inner{max-width:100%}`、`.hero h1{clamp(2.2rem,9.5vw,3.6rem);word-break:break-word}`、`.hero-sub`、`.hero-actions{margin-top:36px;gap:12px}`、`.section{padding:74px 20px}`、`.feat{padding:26px 20px}`、`.modal-window{margin:auto}`（修溢出裁顶）
  - 430 块：`.hero h1{clamp(2rem,8.5vw,3rem)}`、`.section{padding:64px 16px}`、`.feat/.footer/.modal/.modal-body/.social-card` 收 padding；**关键**：`--blur` 14px 覆盖因级联顺序（760 块在 430 块之后也设了 18px）必须放在 760 FX 块之后独立 `@media (max-width:430px)` 才能生效，已修正
- 验证：CSS 括号平衡 182/182；grep 无 `100vw`；移动端专属规则均落位（word-break / modal-window margin:auto / 14px blur）
